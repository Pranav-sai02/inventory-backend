server:
  port: 8081

spring:
  application:
    name: avoota_inventory
  datasource:
    url: jdbc:mysql://localhost:3306/avoota
    username: root
    password: Count02
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  api-docs:
    path: /v3/api-docs
